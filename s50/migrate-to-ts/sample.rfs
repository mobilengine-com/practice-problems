server program sample for schedule * * * * * first run at 2100-01-01 00:00

using rfs sample;

{
    foreach (var x in [1,2,3]) {
        trace x;
    }

    var myDtf = (dtf yyyy "-\"-" MM "-\"-" dd);

    if (1 == 0) {
        Error("Math is broken");
    }

    var myAddFunction = function(var p1, var p2) {
        return p1 + p2;
    };
    trace "2+2 is ", myAddFunction(2, 2);

    var myMap = {a: 1, b: 2};
    var myMapA = myMap?["a"];
    myMap?["a"] = 42;

    trace "Done";
}