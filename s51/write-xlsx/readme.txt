Write an rfs which writes the list of files and directory into a spreadsheet.
Format header differently from body. Use yyyy-MM-dd HH:mm:ss formatting on dates.

Put xlsx into a dedicated directory in the solution.